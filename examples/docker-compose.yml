version: "3.7"

services:

  webapp:
    container_name: uploadprogress-example
    image: uploadprogress-example
    build:
      context: ./.docker
      args:
        php_version: 8.0
        uploadprogress_version: 2.0.1
    ports:
      - "8080:80"
    volumes:
      - "./.tmp:/tmp"
      - ".:/var/www/html"
